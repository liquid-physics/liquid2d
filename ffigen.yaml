# Run with `flutter pub run ffigen --config ffigen.yaml`.
name: ChipmunkBinding
description: |
  Bindings for `tool/header.h`.

  Regenerate bindings with `flutter pub run ffigen --config ffigen.yaml`.
output: 'tools/chipmunk_binding.dart'
headers:
  entry-points:
    - 'tools/header.h'

